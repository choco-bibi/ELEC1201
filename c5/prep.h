// weh why is there so much skeleton code
// just going to enter code I wrote

// in matrix.h
typedef struct {
	int rows;
	int cols;
	double **element;
} Matrix;

//in vector.h
typedef struct {
	int length;
	double *element;
} Vector;
